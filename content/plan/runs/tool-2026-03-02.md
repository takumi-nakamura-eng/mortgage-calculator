# tool weekly run (2026-03-02)

## 実施内容
- `content/plan/next_week.json` がリポジトリ内に存在しなかったため、`tool` 未指定扱いで柱3本改善のフォールバックを適用。
- `bolt-length`（`/tools/bolt`）を改善。
- 追加内容:
  - 入力プリセットを4種追加（鋼板固定 / 機械ベース固定 / 厚板締結 / 軽量カバー固定）
  - プリセット選択で呼び径・厚さ・ナット/座金枚数・用途メモを即時反映
  - ナット/座金枚数の整数・範囲（0〜10）バリデーションを追加し、入力エラーを明示

## 差別化ポイント（3点）
1. 代表的な締結シーンを1クリックで再現できるプリセットにより、初期入力コストを削減。
2. 枚数入力の静かな補正（暗黙丸め）を廃止し、整数/範囲エラーを明示して誤計算を防止。
3. 用途メモまでプリセット連動させ、履歴・PDFの文脈情報を最初から整える運用に改善。

## ベースラインチェック
- 入力プリセット >=4: **OK**（4種）
- 途中式（式展開＋代入値＋結果）: **OK**（既存ロジック維持）
- 履歴（/history）: **OK**（既存 `addEngHistoryEntry` 維持）
- PDF出力（A4：入力・途中式・結果・免責）: **OK**（既存 `printEngReport` 維持）
- 単位表記: **OK**（mm / 枚 をUIで表示）
- エラー表示: **OK**（締結厚さ＋枚数入力を明示）
- 関連記事/関連ツール導線: **OK**（ツールページ既存の関連記事導線を維持）

## 次の改善案（翌週候補）
- `simple-beam` に荷重ケース別プリセット（機器据付・配管支持・作業床）を追加し、入力ミスを減らす。

## 追記（2026-03-02 追加改善）
- プリセットの呼び径・枚数に出典紐付けがないため、`bolt-length` の用途名プリセットを一旦撤去。
- `lib/bolts/specs.ts` を追加し、径別計算仕様（`BOLT_CALC_SPECS`）をUIから分離。
- M12基準（JIS系代表寸法）の概略図を `bolt-length` 画面へ追加。
  - 表示: `t / Hnut / Hpw / Hsw / 3p` の位置
  - 実寸ラベル: `s / k / d / ODpw / tpw / ODsw / tsw / 3p`
- PDF計算書にも同等のM12概略図を追加（①図解→②入力→③計算式→④計算結果）。
- 免責へ追記: 図解寸法はM12概略で製品寸法差を含む。

### 追加改善後のベースライン再確認
- 入力プリセット >=4: **適用外**（根拠不足のため撤去）
- 途中式（式展開＋代入値＋結果）: **OK**
- 履歴（/history）: **OK**
- PDF出力（A4：入力・途中式・結果・免責）: **OK**（図解セクション追加）
- 単位表記: **OK**
- エラー表示: **OK**
- 関連記事/関連ツール導線: **OK**
