# weekly-plan run (2026-03-02)

## Top20要約（データ件数: 8）

- 1位 `bolt-length` 87.5
- 2位 `simple-beam` 83.0
- 3位 `section-properties` 81.7
- `anchor` は 64.7 で conditional 閾値未達
- 実務上の改善優先帯は `avgPosition 8-20` の既存主力3テーマ

## mode選択理由

- 選択: `improve`
- 根拠:
  - 既存資産（tools: bolt-length/simple-beam/section-properties）で需要上位を占有
  - CTR改善余地（低CTRかつ順位8-20）が明確
  - 新規 create 候補は score>=70 かつ差別化ゲート通過案件が不在

## next_week.json 要約

- `weekOf`: `2026-03-02`
- `mode`: `improve`
- `articles`: 1件（`deflection-limit-l-over-n`）
- `tool`: 1件（`bolt-length`）
- `focusPillars`: `bolt-length`, `simple-beam`, `section-properties`
- `conditional.anchor`: false（`score=64.7`）

## 補足

- `scripts/demand-report.ts` は Node直実行できるよう import を相対パス化し、
  `node --experimental-strip-types scripts/demand-report.ts` で採点可能にした。
- `git pull origin main` はネットワーク制限で未実施（要後日同期）。

